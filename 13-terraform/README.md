# Deploying pre-build wcg container

- Replicas can be set via ```replica_num``` var in ```terraform.tfvars```
- Default ingress hostname **wcg-app-terraform.io**
- Access to app **wcg-app-terraform.io/tfapp**